run:
	./echo $(TN_ARGS)

build: echo

print-nf-name:
	echo "Simple NF/IO_uring test"

echo: main.c pci.c
	gcc main.c pci.c -O2 -I$(LIBURING_PATH)/include -L$(LIBURING_PATH)/lib -luring -o echo

is-linux: ;